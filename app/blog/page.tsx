import Link from "next/link";
import { ArrowRight } from "lucide-react";
import { articles } from "@/lib/marketing-content";
import { SiteFooter } from "@/components/site-footer";
import { SiteHeader } from "@/components/site-header";
export const metadata = { title: "Local SEO, Maps & AI Search Guides", description: "Practical guides for map listings, multi-location SEO, reviews, answer engine optimisation and local content planning.", alternates: { canonical: "/blog" } };
export default function BlogIndex() { const stages = ["Foundation", "Visibility", "Conversion", "Scale"] as const; return <><SiteHeader /><main><section className="bg-slate-50 px-5 py-20 text-center"><p className="text-sm font-bold uppercase tracking-[.15em] text-blue-700">11i Maps resource hub</p><h1 className="mx-auto mt-3 max-w-3xl text-5xl font-semibold tracking-tight">Local growth guides for every stage of the journey.</h1><p className="mx-auto mt-5 max-w-2xl text-lg leading-8 text-slate-600">From accurate listings to answer-ready content, use these practical articles to build a local presence customers can trust.</p></section>{stages.map((stage) => <section key={stage} className="mx-auto max-w-7xl px-5 py-12"><div className="mb-6 flex items-baseline justify-between"><h2 className="text-2xl font-semibold">{stage}</h2><p className="text-sm text-slate-500">{stage === "Foundation" ? "Get the facts right." : stage === "Visibility" ? "Help customers discover you." : stage === "Conversion" ? "Make choosing easier." : "Create a repeatable process."}</p></div><div className="grid gap-5 md:grid-cols-2 xl:grid-cols-3">{articles.filter((a) => a.stage === stage).map((article) => <article key={article.slug} className="flex flex-col rounded-2xl border border-slate-200 bg-white p-6"><p className="text-xs font-bold uppercase tracking-[.13em] text-blue-700">{article.keywords[0]}</p><h3 className="mt-3 text-xl font-semibold leading-7">{article.title}</h3><p className="mt-3 grow text-sm leading-6 text-slate-600">{article.description}</p><Link href={`/blog/${article.slug}`} className="mt-6 inline-flex items-center gap-2 text-sm font-semibold text-blue-700">Read guide <ArrowRight className="h-4 w-4" /></Link></article>)}</div></section>)}</main><SiteFooter /></>; }
