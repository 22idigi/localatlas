import { auth } from "@/auth";
import { prisma } from "@/lib/prisma";
export default async function SettingsPage() { const session = await auth(); if (!session?.user?.id) return null; const user = await prisma.user.findUnique({ where: { id: session.user.id }, select: { brandVoice: true, supportEmail: true } }); return <main className="px-6 py-8 md:px-10"><header><p className="text-sm text-zinc-500">Workspace settings</p><h1 className="mt-1 text-3xl font-semibold tracking-tight">Set the voice your customers hear.</h1></header><section className="mt-8 max-w-2xl rounded-xl border border-zinc-200 bg-white p-5 dark:border-zinc-800 dark:bg-zinc-900"><label className="block text-sm font-medium">Brand voice<textarea defaultValue={user?.brandVoice ?? ""} className="mt-2 min-h-24 w-full rounded-lg border border-zinc-200 bg-transparent p-3 text-sm dark:border-zinc-700" /></label><label className="mt-5 block text-sm font-medium">Support email<input defaultValue={user?.supportEmail ?? ""} type="email" className="mt-2 w-full rounded-lg border border-zinc-200 bg-transparent p-3 text-sm dark:border-zinc-700" /></label><button className="mt-6 rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white">Save preferences</button></section></main>; }
